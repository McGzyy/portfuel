/** @deprecated Replaced by fixed bottom tab bar in MemberNav on mobile. Kept for call-site compatibility. */
export function WorkspaceQuickActions(_props?: {
  compact?: boolean;
  proUnlocked?: boolean;
}) {
  return null;
}
