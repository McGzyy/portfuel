import { TickerPageSkeleton } from "@/components/ticker/TickerPageSkeleton";

/** Content-only skeleton — workspace chrome comes from the parent layout. */
export default function TickerLoading() {
  return <TickerPageSkeleton />;
}
